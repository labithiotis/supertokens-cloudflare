import { HapiWrapper } from "./framework";
const plugin = HapiWrapper.plugin;
const wrapRequest = HapiWrapper.wrapRequest;
const wrapResponse = HapiWrapper.wrapResponse;
export {
  plugin,
  wrapRequest,
  wrapResponse
};
